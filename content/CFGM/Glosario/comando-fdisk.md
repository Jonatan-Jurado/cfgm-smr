---
title: "Comando fdisk"
---
# Comando fdisk
Es un potente editor de particiones de disco. En tareas de verificación de [[Hardware]], se utiliza con el parámetro `-l` (`sudo fdisk -l`) para listar todas las particiones existentes en las unidades de almacenamiento conectadas al sistema.